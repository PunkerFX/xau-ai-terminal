import { InstitutionalBrief } from '@/institutional-brief';
export class PromptBuilder { buildMarketAnalysisPrompt(brief: InstitutionalBrief): string { return `You are an institutional gold analyst. Market Data: XAUUSD Bias: ${brief.marketBias} Confidence: ${brief.confidence}% Conviction: ${brief.conviction}% Engines: Macro: ${brief.engines.macro}/100 Institutional: ${brief.engines.institutional}/100 Technical: ${brief.engines.technical}/100 Top Risks: ${brief.topRisks.join(', ')} Top Opportunities: ${brief.topOpportunities.join(', ')} Provide a concise institutional summary (3-5 sentences) of the current gold market outlook.`; } buildRiskAssessmentPrompt(context: Record<string, unknown>): string { return `Assess the risk of the following gold trading setup:\n${JSON.stringify(context, null, 2)}\nProvide risk level (LOW/MEDIUM/HIGH/EXTREME) and justification.`; } }
